{% extends 'main.html.twig' %}

{% block body %}
    <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
                        aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="{{ services['config']['baseDir'] }}">Puppy</a>
            </div>
            <div id="navbar" class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li><a href="{{ services['config']['baseDir'] }}contact">Form demo</a></li>
                    <li><a href="{{ services['config']['baseDir'] }}redirect-from">Redirection demo</a></li>
                </ul>
                <form class="navbar-form navbar-right" role="form" action="{{ services['config']['baseDir'] }}">
                    <div class="form-group">
                        <input type="text" name="flash-message" placeholder="woof!" class="form-control">
                    </div>
                    <button type="submit" class="btn btn-success">Flash</button>
                </form>
            </div>
            <!--/.navbar-collapse -->
        </div>
    </nav>

{% block content %}{% endblock %}

{% endblock %}
